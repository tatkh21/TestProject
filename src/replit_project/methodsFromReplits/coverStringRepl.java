package replit_project.methodsFromReplits;

public class coverStringRepl {
    public static void main(String[] args) {
        String main = "java methods";
        String coverMe = "me";
        System.out.println(coverString(main, coverMe));

    }

    public static String coverString(String mainStr, String coverME) {
        String newStr = "";
        if (mainStr.contains(coverME)) {

            for (int i = 0; i < mainStr.length(); i++) {

            }
        } return null;
    }
}
